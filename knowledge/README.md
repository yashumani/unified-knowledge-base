# Knowledge Directory

This directory contains human-editable, Git-friendly examples of brain objects.

The long-term pattern is:

```text
humans edit Markdown/YAML
  -> compiler validates
  -> review workflow approves
  -> runtime publishes to store/indexes
```

Use synthetic examples only in this public repository.
