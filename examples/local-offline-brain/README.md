# Local Offline Brain Example

This example shows the intended shape of a brain package that can run inside a restricted or offline environment.

It uses:

- local YAML/Markdown files
- local plugins
- human review before publish
- optional local AI configuration

No real company data should be placed in this example.
